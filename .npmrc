# npm 10.x crashes resolving vitest 4's optional browser peer deps from scratch
# ("Cannot read properties of null (reading 'edgesOut')" in arborist). The
# resolved tree is fine — `npm ci` from the committed lockfile works without
# this. The flag only affects a fresh `npm install`. Revisit when npm is fixed.
legacy-peer-deps=true
